import { storiesOf } from '@storybook/react-native';
import React from 'react';

import RevealingSubheader from '../../../src/screens/RevealingSubheader';

storiesOf('RevealingSubheader', module).add('Default', () => (
  <RevealingSubheader />
));
